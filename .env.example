# local — можно передавать customer_tg_id без initData (отладка в браузере).
# production — только подписанный initData (Mini App / Telegram).
APP_ENV=local
# Telegram WebApp кнопка требует HTTPS.
# Для локальной отладки удобно использовать cloudflared:
#   cloudflared tunnel --url http://127.0.0.1:8000
# и затем поставить сюда выданный https://*.trycloudflare.com
BASE_URL=http://127.0.0.1:8000

# Uvicorn: по умолчанию 127.0.0.1:8000. Если в браузере «не открывается» при
# адресе localhost — откройте http://127.0.0.1:8000 (Windows часто шлёт localhost на IPv6).
# API_HOST=0.0.0.0 — слушать на всех интерфейсах (удобно с телефона в LAN).
# API_HOST=127.0.0.1
# API_PORT=8000

# SQLite for local development
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Telegram bot token (keep empty for now)
BOT_TOKEN=8505123303:AAEHClvs0hKIzYi62MkJHzIA5lXctCwsl10

# Where operator notifications will be sent (username is a starting point;
# later we will resolve and store numeric Telegram ID)
OPERATOR_USERNAME=@Viktor5965
# Рекомендуется вместо username использовать числовой chat_id (надежнее).
# Его можно узнать командой /id в боте.
# Без OPERATOR_CHAT_ID бот не пришлёт оператору кнопку «Передан в доставку» после оплаты.
OPERATOR_CHAT_ID=830678989

# Проверка подписи Mini App initData (HMAC по документации Telegram).
# Пусто — используется BOT_TOKEN. Ненужно дублировать, если токен уже в BOT_TOKEN.
TELEGRAM_WEBAPP_SECRET=
# Сколько секунд жить auth_date в initData (защита от повторной отправки старой строки).
# WEBAPP_INIT_MAX_AGE_SEC=86400

# Веб-панель оператора: https://ВАШ_ДОМЕН/operator-panel
# HTTP Basic: логин operator, пароль = этот токен. Пусто — URL панели отдаёт 404.
OPERATOR_PANEL_TOKEN=DbrnjhNtgkzrjd

